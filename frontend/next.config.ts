import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
      domains: ['rafhael', '192.168.0.77', 'raw.githubusercontent.com', 'localhost', 'bancodetalentos.s3.us-east-1.amazonaws.com'],
    },
};

export default nextConfig;
